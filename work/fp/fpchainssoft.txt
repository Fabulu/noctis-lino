      ( *** fpchains - GENERATED, do not edit ***

	generated by tools/genfp.py from fpsched.txt
	backend: soft

	Checked in on purpose, so a reviewer reads bytes rather than a
	generator.  Every routine below is ONE fragment: the intermediate
	never leaves the x87 register stack, which is the whole point.
	A scalar route through fpx87 would store - and round - after each
	operation, and one such store costs about a quarter of the
	STARMAP records.

	Each routine reads the fpabi slots named in its header and leaves
	its result in FA0/FA1.  None of them touches A/B/C/D/E or edi.
	None of them sets the control word: call FEnter first. )

"variables"

	( NsIdentity )
	FKNsIdentityK100000	= 100000;

	( NsIdentityPermuted )
	FKNsIdentityPermutedK100000	= 100000;

"programme"

( -------------------------------------------------------------------- )

      ( NsIdentity
	NOCTIS-0.CPP:4078 nearstar_identity = x/100000*y/100000*z/100000
	left associated exactly as C requires: five operations, ONE store

	int32 in : x=FJ0, y=FJ1, z=FJ2
	f64   out: id = FA0/FA1
	control  : 133Fh
	exact    : YES
	x87 depth: peak 2, stores 1
	chain id : 1 )

"NsIdentity"
	=> XReset;
	A = [FJ0]; [XIN] = A;  => XFild;	(fild x)
	A = [FKNsIdentityK100000]; [XIN] = A;  => XFiquo;	(fidiv K100000)
	A = [FJ1]; [XIN] = A;  => XFild;	(fild y)
	=> XFmulp;				(fmulp)
	A = [FKNsIdentityK100000]; [XIN] = A;  => XFiquo;	(fidiv K100000)
	A = [FJ2]; [XIN] = A;  => XFild;	(fild z)
	=> XFmulp;				(fmulp)
	A = [FKNsIdentityK100000]; [XIN] = A;  => XFiquo;	(fidiv K100000)
	=> XFstpQ;				(fstp id)
	end;

( -------------------------------------------------------------------- )

      ( NsIdentityPermuted
	z,y,x instead of x,y,z - multiplication is commutative and the
	divisions are by the same constant, so the value is unchanged

	int32 in : x=FJ0, y=FJ1, z=FJ2
	f64   out: id = FA0/FA1
	control  : 133Fh
	exact    : YES
	x87 depth: peak 2, stores 1
	chain id : 2 )

"NsIdentityPermuted"
	=> XReset;
	A = [FJ2]; [XIN] = A;  => XFild;	(fild z)
	A = [FKNsIdentityPermutedK100000]; [XIN] = A;  => XFiquo;	(fidiv K100000)
	A = [FJ1]; [XIN] = A;  => XFild;	(fild y)
	=> XFmulp;				(fmulp)
	A = [FKNsIdentityPermutedK100000]; [XIN] = A;  => XFiquo;	(fidiv K100000)
	A = [FJ0]; [XIN] = A;  => XFild;	(fild x)
	=> XFmulp;				(fmulp)
	A = [FKNsIdentityPermutedK100000]; [XIN] = A;  => XFiquo;	(fidiv K100000)
	=> XFstpQ;				(fstp id)
	end;

